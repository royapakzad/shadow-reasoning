
// constants/api.ts

/**
 * Defines the names of the environment variables used to store API keys in `env.js`.
 * This provides a single source of truth for these key names.
 */
export const TOGETHER_API_KEY_ENV_VAR = "TOGETHER_API_KEY";